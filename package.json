{
  "name": "meta-ibd",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build && cd .. && node -e \"const fs=require('fs'); fs.cpSync('client/dist', 'public', {recursive:true})\""
  }
}
